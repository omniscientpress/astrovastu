# Drop your real logo here

Save your file as:

  public/images/logo.png

Then restart `npm run dev`. The site automatically prefers logo.png over the SVG fallback.

Recommended: square PNG, transparent background if possible, at least 512×512.
