import type { Testimonial } from './types'

/**
 * PLACEHOLDER — replace with real client reviews before launch.
 * Nothing here is a real client quote; these exist only so layout can be built.
 * Never invent client counts, ratings, or outcome percentages anywhere on the site.
 */
export const testimonials: Testimonial[] = []
